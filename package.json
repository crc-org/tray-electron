{
  "name": "tray-electron",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "release": "electron-packager . --overwrite --out=release",
    "release:cross": "electron-packager . --overwrite --out=release --platform=darwin,linux,win32",
    "dev:serve": "concurrently \"electron .\" \"cd src && npm start\""
  },
  "author": "",
  "license": "Apache",
  "devDependencies": {
    "concurrently": "^6.4.0",
    "electron": "^13.1.7",
    "electron-packager": "^15.3.0"
  },
  "dependencies": {
    "got": "^9.6.0"
  }
}
